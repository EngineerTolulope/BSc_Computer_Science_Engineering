#!/usr/bin/env python
# coding: utf-8 

#The program get a name from the user and store in the string simple, then the function capitalize is called with parameter simple, which capitalizes all the letters and stores it back in simple, then the new string is printed to the screen with a welcome message
import sys

def capitalize(simple):
    simple=simple.upper()
    print "{n}, You are welcome to T.K's Kitchen".format(n=simple)
 
def main():
    simple=raw_input("Your Name Please:- ")
    capitalize(simple)
       
    
if __name__=="__main__":
    main()
